#!/usr/bin/python3
def number_keys(a_dictionary):
    '''
    Returns the number of keys in a dictionary
    Parameters:
    a_dictionary: Dictionary with keys
    '''
    m = len(a_dictionary.keys())
    return m
